
from typing import List


def threeSum(nums: List[int]) -> List[List[int]]:
    # First of all sort the array 
    nums.sort()
    result = []

    # itterathe through the array to freeze one of the elements
    


if __name__ == "__main__":
    # Example usage
    nums = [-1, 0, 1, 2, -1, -4]
    print(threeSum(nums))  # Expected output: [[-1, -1, 2], [-1, 0, 1]]
    # Note: The actual implementation of threeSum is not provided in this snippet.